/*
You're creating a basic login system. Make a login function with two things: a username and a
password. Check if the username is "admin" and the password is "12375". If they're both correct, 
'how "Login successful"; if not, show "Invalid credential'."

*/
let userId = "admin"
let pass = 12375

if(userId == "admin" && pass == 12375){
     console.log("Login Successful")
}else{
    console.log("Invalid Credentail")
}